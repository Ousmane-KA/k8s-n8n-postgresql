apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-postgres
  namespace: automate-n8n
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/postgres
  persistentVolumeReclaimPolicy: Retain
